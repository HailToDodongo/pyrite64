# TinySQ TODO:

- MP3 input support
- Sample resampling: `tsq_conv_wav` has no resample option
- Sample loop points: looped samples work (baked via the per-asset "Loop" option,
  played as persistent SFX with voice revival). Only full-sample loops can be baked from
  the editor so far; consider a loop-start/length asset setting.

## Nice to have:
- Sequence/sound-font preview: the asset inspector has no host-side preview for `.mid`/`.xm`/`.sf2`
